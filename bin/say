#!/usr/bin/env bash
S="${AGENT_SHARED:-$HOME/.agent-grid/shared}/conversation.log"; mkdir -p "$(dirname "$S")"
printf '[%s] %s: %s\n' "$(date -u +%H:%M:%S)" "${AGENT_NAME:-user}" "$*" >> "$S"; echo posted.
